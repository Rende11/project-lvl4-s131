extends /layouts/application.pug

block content
  +jumb  
    +formFor(form, errors, { url: urlFor('statusNew'), method: 'post' })
      +input(form, errors, 'name', { label: 'Status name' })
      +button(form)