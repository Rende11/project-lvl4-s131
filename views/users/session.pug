extends /layouts/application.pug

block content
  +formFor(form, errors, { url: urlFor('session'), method: 'post' })
    +input(form, errors, 'email', { label: 'Email' })
    +input(form, errors, 'password', { label: 'Password', as: 'password' })
    +button(form)
